[package]
name = "crustcore-index"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Optional repo memory / code intelligence for CrustCore."

[dependencies]
crustcore-types = { workspace = true }
crustcore-secrets = { workspace = true }
# TODO(P14): the std-only retrieval/compaction core (repo map/capsule, grep
# code-intel, memory store, redacted+bounded context selection) is implemented.
# Still deferred (all optional, never linked into nano): the live
# `git ls-files`/`git grep` invocation (`TODO(P14-exec)`), a persistent
# SQLite/redb store (`TODO(P14-store)`), and AST/tree-sitter/LSP/embeddings
# code-intel (`TODO(P14-intel)`). Memory is retrieved context, never authority.
