# tests/fixtures/

Shared inputs for the red-team and golden suites: sample repositories, malicious
path lists, mock model/worker transcripts, mock MCP responses, and mock GitHub
payloads.

**Never commit a real secret here** (invariants 1–3). Use obvious placeholders
such as `EXAMPLE_TOKEN_DO_NOT_USE`.
